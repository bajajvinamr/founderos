# Retrospection — 2026-04-23

First retrospection pass on FounderOS. Ran `/retrospect-project` from a clean working tree with prod healthy.

## Velocity

- **87 commits** in last 14 days (2026-04-09 → 2026-04-23). Wave-8 → Wave-23A/B/C/D + 5 fixes.
- **1564 / 1569 tests passing** (3 skipped, 2 flaky). Typecheck green on server, UI, packages.
- **Build status:** green. Fly + Vercel both return 200. `/api/health/deep` status `ok` across all 5 checks.

## What we changed in this pass

| Change | Commit |
|---|---|
| Created project `CLAUDE.md` (66 lines, distilled from `AGENTS.md` + `CONTINUE.md` + workflows README) | this retro |
| Deleted `.github/workflows/pr.yml` (legacy policy gate, superseded by `ci.yml`) | this retro |
| Deleted `.github/workflows/release.yml` (targeted nonexistent `master` branch) | this retro |
| Renamed `e2e.yml` → `e2e-manual.yml` (disambiguated from `e2e-ci.yml`) | this retro |
| Updated `.github/workflows/README.md` to reflect the 3 workflow changes | this retro |
| Marked `docs/plans/2026-03-13-issue-documents-plan.md` as **SHIPPED** (verified routes + tests exist) | this retro |
| Added `agent-instructions-routes.test.ts` to `docs/CI-KNOWN-FLAKES.md` (new flake signal) | this retro |
| Deleted local `dev` branch (60+ commits behind `main`, unmaintained — user-approved) | n/a (git op) |

## What we observed but didn't fix

| Observation | Why deferred |
|---|---|
| **3 files over 4,000 lines** — `server/src/services/heartbeat.ts` (4866), `company-portability.ts` (4415), `ui/src/pages/AgentDetail.tsx` (4134) | Refactors at this scale need their own planning pass. Tracked for a future session. |
| **Composio client still on deprecated v1 API** | Requires either installing `@composio/core` SDK or a real-key probe against v3 to confirm body shapes. Already documented in `CONTINUE.md` with two unblock options. |
| **17 in-code TODOs** | All are legitimate tagged future work (stripe stubs, permission async approval, skills UI re-enable, adapter fallback removal). No rot to prune. |
| **2–3 flaky tests per run** | Root cause is shared embedded-PG data dir under parallel load. Proper fix needs per-file DB fixtures — real engineering work, not audit work. Quarantined in `docs/CI-KNOWN-FLAKES.md`. |

## Delta to workflow

| Metric | Before | After |
|---|---|---|
| Project `CLAUDE.md` lines | 0 (file missing) | 66 |
| Workflows in `.github/workflows/` | 19 | 17 |
| Dead branches | 1 (`dev`) | 0 |
| Known-flakes documented | 2 | 3 |
| Stale "Draft" plans in `docs/plans/` | 1 | 0 (marked SHIPPED) |
| Master-branch references in CI | 2 workflows | 0 |

## Observations for next retro

- **Wave cadence is strong.** 23 waves in ~2 weeks with ADR + PRD + CHANGELOG discipline. Don't regress this.
- **Workflow sprawl will recur.** 17 is still a lot. Any future wave that adds a workflow should justify against merging into an existing one.
- **Composio v3 migration is debt that ages badly.** Every day we don't migrate is a day a new user could hit the 410 wall. If active users ramp, this becomes urgent immediately.
- **Flaky-test count is creeping** (2 → 3 this retro). The fix is infrastructure (isolated DB per test file), not patching quarantine-after-quarantine. Budget a session for it before the count hits 5.
- **Large files compound.** `heartbeat.ts` has doubled since wave-18. Consider setting a soft gate (warn, don't block) in CI at 1500 lines.
- **No retro before this one.** 5+ weeks of wave shipping without retrospection. Keep the cadence at 2–4 weeks from here.

## Next retro target

**2026-05-14** (3 weeks out). Suggested focus areas based on observations above: flaky test infra, largest-file refactors, composio v3 migration status.
