# Vocabulary Swap List — for TC04 to apply

_Generated by TC01 (vocabulary purge audit) on 2026-05-13._

## Summary

- P0 swaps: 18
- P1 swaps: 24
- P2 swaps: 8
- Total: 50
- Test-blocking swaps: 8

## P0 — Founder sees in first 30 minutes (apply first)

| File | Line | Current | Proposed | Test selector concern? |
|---|---|---|---|---|
| ui/src/pages/InstanceSettings.tsx | 171 | "Team Check-ins" | "Team Check-ins" (already changed from Heartbeats) | No — internal; already renamed |
| ui/src/pages/InstanceSettings.tsx | 174 | "Agents with a team check-in enabled" | Keep as-is | No — already founder-friendly |
| ui/src/pages/InstanceProvidersSettings.tsx | 73 | "The fastest path is the Claude Code CLI" | "The fastest path is the Claude Code desktop app" | No — Landing copy |
| ui/src/pages/InstanceProvidersSettings.tsx | 97 | "CLI (subscription):" | "Desktop app (subscription):" | No — provider label |
| ui/src/pages/InstanceProvidersSettings.tsx | 108 | "defaults to CLI but automatically falls back" | "defaults to desktop app but automatically falls back" | No — provider explanation |
| ui/src/pages/InstanceProvidersSettings.tsx | 166 | "Local CLI" | "Desktop app" | No — provider type label |
| ui/src/pages/RunnerStatusPill.tsx | 84 | "Runner online" | "Your desktop connector is online" | Yes — `runner-status-pill.test.ts` asserts label text |
| ui/src/pages/RunnerStatusPill.tsx | 86 | "Runner stale" | "Your desktop connector is offline" | Yes — test assertion |
| ui/src/pages/RunnerStatusPill.tsx | 88 | "Install runner" | "Set up your desktop connector" | Yes — test assertion |
| ui/src/pages/RunnerStatusPill.tsx | 93 | "Checking runner…" | "Checking your desktop connector…" | Yes — test assertion |
| ui/src/pages/Auth.tsx | 125 | "Enter your email to get a magic link." | "Enter your email to get a one-click sign-in link." | No — error message |
| ui/src/pages/Auth.tsx | 137 | "Magic link sent. Check your email." | "One-click sign-in link sent. Check your email." | No — toast message |
| ui/src/pages/Auth.tsx | 138 | "Magic link sent" | "Sign-in link sent" | No — toast title |
| ui/src/pages/Auth.tsx | 320 | "Send me a magic link instead" | "Send me a sign-in link instead" | No — button label |
| ui/src/pages/Landing.tsx | 1006 | "BYO provider keys" | "Use your own provider keys" | No — pricing page |
| ui/src/pages/Landing.tsx | 1013 | "— BYO provider keys" | "— Use your own provider keys" | No — pricing page |
| ui/src/pages/Landing.tsx | 1093 | "Every tier is BYO provider keys" | "Every tier uses your own provider keys" | No — FAQ |
| ui/src/pages/OnboardingWizardNew.tsx | 184 | "Connect an AI provider" | "Connect an AI provider" (keep) | No — already founder-friendly |

## P1 — First session (Agent detail, Approvals, Daily brief)

| File | Line | Current | Proposed | Test selector concern? |
|---|---|---|---|---|
| ui/src/pages/AiConnections.tsx | 216 | "Link to /instance/settings/adapters" | Keep link structure | No — internal routing |
| ui/src/pages/InstanceProvidersSettings.tsx | 61 | "Each provider can be…" | Keep as-is | No — already founder-friendly |
| ui/src/pages/InstanceProvidersSettings.tsx | 399 | "Runs agents via the `claude` CLI" | "Runs agents via the Claude desktop app" | No — provider description |
| ui/src/pages/InstanceProvidersSettings.tsx | 410 | "Runs agents via the `codex` CLI" | "Runs agents via the Codex desktop app" | No — provider description |
| ui/src/pages/InstanceProvidersSettings.tsx | 421 | "Runs agents via the `gemini` CLI" | "Runs agents via the Gemini desktop app" | No — provider description |
| ui/src/pages/InstanceProvidersSettings.tsx | 424 | "@google/gemini-cli" | "@google/gemini-cli" (keep — internal) | No — package name |
| ui/src/pages/CliAuth.tsx | 48 | "Invalid CLI auth URL." | "Invalid sign-in request URL." | No — error message |
| ui/src/pages/CliAuth.tsx | 52 | "Loading CLI auth challenge..." | "Loading sign-in request..." | No — loading message |
| ui/src/pages/CliAuth.tsx | 59 | "CLI auth challenge unavailable" | "Sign-in request unavailable" | No — error heading |
| ui/src/pages/CliAuth.tsx | 77 | "CLI access approved" | "Sign-in approved" | No — success heading |
| ui/src/pages/CliAuth.tsx | 79 | "The FounderOS CLI can now finish authentication" | "The FounderOS desktop app can now finish sign-in" | No — success message |
| ui/src/pages/CliAuth.tsx | 94 | "CLI auth challenge expired" | "Sign-in request expired" | No — error message |
| ui/src/pages/CliAuth.tsx | 97 | "Start the CLI auth flow again" | "Start the sign-in flow again" | No — error message |
| ui/src/pages/CliAuth.tsx | 110 | "return to this page to approve the CLI access request" | "return to this page to approve the desktop app sign-in" | No — error message |
| ui/src/pages/CliAuth.tsx | 123 | "Approve FounderOS CLI access" | "Approve FounderOS desktop app sign-in" | No — page heading |
| ui/src/pages/CliAuth.tsx | 125 | "A local FounderOS CLI process is requesting board access" | "Your FounderOS desktop app is requesting board access" | No — explanation |
| ui/src/pages/CliAuth.tsx | 135 | `{challenge.clientName ?? "founderos cli"}` | `{challenge.clientName ?? "founderos desktop app"}` | No — client label |
| ui/src/pages/CliAuth.tsx | 170 | "Approve CLI access" | "Approve desktop app access" | No — button label |
| ui/src/pages/RunnerInstallDialog.tsx | 148 | "Label" (for runner token) | "Label" (keep — generic) | No — form field |
| ui/src/pages/RunnerInstallDialog.tsx | 246 | "runner-issued-banner" (data-testid only) | Keep data-testid unchanged | No — only internal test ID |
| ui/src/pages/Permissions.tsx | 80 | "Autonomy matrix" | "Authority matrix" | No — settings page |
| ui/src/pages/Permissions.tsx | 123 | "Autonomy levels" | "Authority levels" | No — settings page |
| ui/src/pages/Permissions.tsx | 241 | `AutonomyBadge` (component name) | Keep as TypeScript identifier | No — internal component |
| ui/src/components/AgentProviderBadge.tsx | 89 | "Claude CLI" | "Claude desktop app" | Yes — `agent-provider-badge.test.ts` may assert label |
| ui/src/components/AgentProviderBadge.tsx | 90 | "Codex CLI" | "Codex desktop app" | Yes — test assertion |

## P2 — Deep pages / admin (Settings, Integrations, Debug)

| File | Line | Current | Proposed | Test selector concern? |
|---|---|---|---|---|
| ui/src/pages/InstanceSettings.tsx | 36 | "Workspace settings" | "Workspace settings" (keep) | No — already generic |
| ui/src/pages/LegalPrivacy.tsx | 30 | "instance database" | "your FounderOS database" | No — legal copy |
| ui/src/pages/LegalPrivacy.tsx | 34 | "Stored in your instance database" | "Stored in your FounderOS database" | No — legal copy |
| ui/src/pages/LegalPrivacy.tsx | 70 | "holds your instance data" | "holds your FounderOS data" | No — legal copy |
| ui/src/pages/ProjectWorkspaceDetail.tsx | 517 | "Setup command" (label) | "Setup command" (keep) | No — technical but used in context |
| ui/src/pages/BoardClaim.tsx | 213 | "Instance Claim" (eyebrow) | "Claim your access" (eyebrow) | No — onboarding page |
| ui/src/pages/BoardClaim.tsx | 214 | "Claim instance admin" | "Claim your founder access" | No — onboarding page |
| ui/src/pages/BoardClaim.tsx | 253 | "this instance" (in generic message) | "your FounderOS account" | No — fallback text |

## Details by term searched

### "Heartbeat(s)" → "Check-in(s)"
- InstanceSettings.tsx already renamed title from Heartbeats to "Team Check-ins" (line 171)
- Remaining instances are internal identifiers (API keys, React query keys, type names) → skip

### "Runner" → "Your desktop connector" or "Local agent"
- **Visible strings found:** RunnerStatusPill.tsx lines 84, 86, 88, 93 (pill labels)
- RunnerInstallDialog.tsx: `data-testid` attributes contain "runner" but these must NOT be changed (test ID contract)
- Other refs are internal (type names, variable names, API paths) → skip

### "CLI" → "Desktop app" (when user-facing)
- InstanceProvidersSettings.tsx lines 73, 97, 108, 166 (provider setup explanations)
- CliAuth.tsx lines 48–170 (the entire CLI auth flow should use "sign-in" instead of "auth" + "desktop app")
- AgentProviderBadge.tsx lines 89–90 (badge labels for provider type)
- Landing.tsx lines 600, 616, 901 (marketing + onboarding copy)
- OnboardingWizard.tsx: references to `${cli.installed}`, `claude` CLI — internal; skip

### "Magic link" → "One-click sign-in link" or "Sign-in link"
- Auth.tsx lines 125, 137–138, 320 (auth UI)
- All instances are user-facing and should be swapped

### "Autonomy" → "Authority"
- Permissions.tsx lines 80, 123 (settings page headings + labels)
- Component name `AutonomyBadge` is internal; skip

### "Instance" (when meaning "your FounderOS") → "Your FounderOS"
- Legal/privacy pages: LegalPrivacy.tsx lines 30, 34, 70
- BoardClaim.tsx lines 213, 214, 253 (onboarding)
- Navigation breadcrumbs say "Instance Settings" but this is lower priority than Heartbeats/Runner/CLI

### "BYO" → "Use your own"
- Landing.tsx lines 1006, 1013, 1093 (pricing + FAQ)
- Legal pages use "BYOK" (Bring Your Own Key) — keep this formal language

### "Adapter" (noun, as in "provider")
- Most refs are internal identifiers: `adapterType`, `getAdapterDisplay()` → skip
- AiConnections.tsx has "adapters API" and link to "adapters" settings — these are already founder-friendly

### "Workspace"
- Already founder-friendly ("Workspace settings" matches Notion convention)
- No swap needed

### "API key" (standalone)
- When context is vague ("Paste API key" without provider name) → "Paste your secret key"
- With context (e.g., "Anthropic API key") → keep context
- InstanceProvidersSettings.tsx line 280: "Paste API key" → "Paste your secret key"

### "Provider strategy"
- OnboardingWizardNew.tsx line 729: "Provider strategy" → "AI setup"

## Notes for TC04 (the apply ticket)

1. **Test assertions:** 8 swaps will require updating test file assertions:
   - `runner-status-pill.test.ts` (lines 84, 86, 88, 93)
   - `agent-provider-badge.test.ts` (lines 89, 90 if it asserts labels)
   - `cli-auth.test.ts` (any assertions on modal headings/messages, if it exists)
   
2. **Do NOT modify:**
   - `data-testid` attributes (e.g., `data-testid="runner-status-pill"`, `data-testid="runner-issued-banner"`)
   - TypeScript variable names, function names, type names (e.g., `adapterType`, `AutonomyBadge`, `HeartbeatAgent`)
   - API route names, DB column names, env var names

3. **Already done:**
   - InstanceSettings.tsx line 171: "Heartbeats" tab already renamed to "Team Check-ins"
   - No swap needed for this one

4. **Coordinate with:**
   - TB05 (Inbox "Heartbeats" rename) — check if that ticket already handled the Inbox tab label
   - TB06 (AgentDetail adapter labels) — those badge labels appear in AgentProviderBadge.tsx and may be a separate ticket

## Files touched by this audit

- ui/src/pages/InstanceSettings.tsx
- ui/src/pages/InstanceProvidersSettings.tsx
- ui/src/pages/RunnerStatusPill.tsx
- ui/src/pages/Auth.tsx
- ui/src/pages/Landing.tsx
- ui/src/pages/CliAuth.tsx
- ui/src/pages/RunnerInstallDialog.tsx
- ui/src/pages/Permissions.tsx
- ui/src/pages/AiConnections.tsx
- ui/src/pages/LegalPrivacy.tsx
- ui/src/pages/ProjectWorkspaceDetail.tsx
- ui/src/pages/BoardClaim.tsx
- ui/src/pages/OnboardingWizardNew.tsx
- ui/src/components/AgentProviderBadge.tsx
