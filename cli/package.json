{
  "name": "founderos",
  "version": "0.3.1",
  "description": "FounderOS CLI — orchestrate AI agent teams to run a business",
  "type": "module",
  "bin": {
    "founderos": "./dist/index.js"
  },
  "keywords": [
    "founderos",
    "ai",
    "agents",
    "orchestration",
    "cli"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/founderos-ai/founderos",
    "directory": "cli"
  },
  "homepage": "https://github.com/founderos-ai/founderos",
  "bugs": {
    "url": "https://github.com/founderos-ai/founderos/issues"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "node --input-type=module -e \"import esbuild from 'esbuild'; import config from './esbuild.config.mjs'; await esbuild.build(config);\" && chmod +x dist/index.js",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^0.10.0",
    "@founderos/adapter-claude-local": "workspace:*",
    "@founderos/adapter-codex-local": "workspace:*",
    "@founderos/adapter-cursor-local": "workspace:*",
    "@founderos/adapter-gemini-local": "workspace:*",
    "@founderos/adapter-opencode-local": "workspace:*",
    "@founderos/adapter-pi-local": "workspace:*",
    "@founderos/adapter-openclaw-gateway": "workspace:*",
    "@founderos/adapter-utils": "workspace:*",
    "@founderos/db": "workspace:*",
    "@founderos/server": "workspace:*",
    "@founderos/shared": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "dotenv": "^17.0.1",
    "commander": "^13.1.0",
    "embedded-postgres": "^18.1.0-beta.16",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
