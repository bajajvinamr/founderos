#!/usr/bin/env node
// OpenAI API HTTP wrapper — scaffold for S7.B.3.
// This stub exists so the package's `bin` entry resolves; it is replaced in S7.B.3
// with the actual stream-json bridge to OpenAI's /v1/chat/completions endpoint.

console.error("[openai-api-runner] scaffold only — implementation lands in S7.B.3");
process.exit(2);
