{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo 'Vercel deploy is now a 301 redirect — no build needed'",
  "outputDirectory": ".vercel-empty",
  "framework": null,
  "installCommand": "echo 'No install needed for redirect-only deploy'",
  "redirects": [
    {
      "source": "/(.*)",
      "destination": "https://founderos.fly.dev/$1",
      "permanent": false
    }
  ]
}
