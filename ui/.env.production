# Pin the API origin used for WebSocket upgrades when the UI is served
# from a different host than the API (Vercel <-> Fly split). Vercel rewrites
# proxy HTTP only — WS upgrades must hit Fly directly. Override per-deploy
# in the Vercel project Environment Variables tab if the API moves.
VITE_API_ORIGIN=founderos.fly.dev
